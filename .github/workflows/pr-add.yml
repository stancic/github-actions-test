name: 'Check if PR contains description'

on: 
  pull_request:
    types: 
      - opened
      - reopened

jobs: 
  pr_opened:
    runs-on: ubuntu-latest
    name: Close PR if there is no description
    steps: 
      - name: What PR contains
        env: 
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
        shell: bash

