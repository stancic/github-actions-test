name: 'Stale check'

# ** What it does? **
#    - Check PRs and issues, and if they're idle for a week, mark them as stale.
#    - Close stale PRs and issues after a week.
# ** Why we have it? **
#    - To have cleaner PR list.
# ** Who does it impact? **
#    - All developers.

on: 
  schedule:
    - cron: '18 13 * * *' # Run each day at 16:00 UTC -> 18:00 CEST (Croatia)

permissions:
  issues: write
  pull-requests: write

jobs:
  stale_check:
    name: 'Check if PR or issue is stale'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bog i hrvati"